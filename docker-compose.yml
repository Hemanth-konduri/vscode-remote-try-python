version: '3.8'

services:
  web:
    build: .
    ports:
      - "9000:9000"
    environment:
      - FLASK_ENV=development
    volumes:
      - .:/app
    command: python -m flask run --host=0.0.0.0 --port=9000 --reload